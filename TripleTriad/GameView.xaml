



<UserControl x:Class="TripleTriad.GameView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:TripleTriad"
      mc:Ignorable="d" 
      >
    <Grid x:Name="RootGrid">
        <Grid.ColumnDefinitions>
            <!--Player 1 Cards-->
            <ColumnDefinition Width="400" />
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="400"/>
        </Grid.ColumnDefinitions>

        <!-- Draggable piece lives here, to the left -->
        <Grid x:Name="WinOverlay" Visibility="Collapsed">
            <Grid.Background>
                <SolidColorBrush Color="pink" Opacity="0.5" />
            </Grid.Background>
        <Border Width="300" Height="200"
                HorizontalAlignment="Center" VerticalAlignment="Center">
                <Border.Background>
                    <SolidColorBrush Color="white" Opacity="0.5" />
                </Border.Background>
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <TextBlock x:Name="WinnerText" FontSize="24" HorizontalAlignment="Center"/>
                <Button Content="Play Again" Click="PlayAgain_Click" Margin="0,20,0,0"/>
            </StackPanel>
        </Border>
    </Grid>
    </Grid>

</UserControl>
