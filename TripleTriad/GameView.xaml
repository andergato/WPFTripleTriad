<UserControl x:Class="TripleTriad.GameView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:TripleTriad"
      mc:Ignorable="d" 
      >
    <Grid x:Name="RootGrid">
        <Grid.ColumnDefinitions>
            <!--Player 1 Cards-->
            <ColumnDefinition Width="200" />
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="200"/>
        </Grid.ColumnDefinitions>

        <Grid x:Name="WinOverlay" Width="300" Height="300" Panel.ZIndex="9" Grid.Column="1" Visibility="Collapsed">
            <Grid.Background>
                <SolidColorBrush Color="pink" Opacity="0.7" />
            </Grid.Background>
            <Border Width="300" Height="300"
                HorizontalAlignment="Center" VerticalAlignment="Center">
                <Border.Background>
                    <SolidColorBrush Color="white" Opacity="0.5" />
                </Border.Background>
                <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                    <TextBlock x:Name="WinnerText" FontFamily="Neo Sans" FontSize="24" HorizontalAlignment="Center"/>
                    <Button Content="Play Again" Click="PlayAgain_Click" FontFamily="Neo Sans" FontSize="25" Margin="0,20,0,0"  Background="#FFC8C9FF"/>
                    <Button Content="Menu" Click="Menu_Click" FontFamily="Neo Sans" FontSize="25" Margin="0,20,0,0"  Background="#FFC8C9FF"/>
                </StackPanel>
            </Border>
        </Grid>

        <Grid x:Name="RulesPage" Panel.ZIndex="10" Grid.Column="1">
            <Grid.Background>
                <SolidColorBrush Color="Pink" Opacity="0.5" />
            </Grid.Background>
            <Border Width="500" Height="500"
        HorizontalAlignment="Center" VerticalAlignment="Center" RenderTransformOrigin="0.509,0.571">
                <Border.Background>
                    <SolidColorBrush Color="white" Opacity="0.5" />
                </Border.Background>
                <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                    <TextBlock x:Name="RulesText" Text="Welcome to Triple Triad!" FontSize="25" HorizontalAlignment="Center" Width="309" Height="62" FontFamily="Segoe UI Black"/>
                    <TextBlock Text="This is a card game based on the Final Fantasy XIV minigame of the same name. In this version, you start with a deck of 5 random cards with one of each star level 1,3,4 and 2 cards of level 2. You and another player will take turns placing your cards onto the board. When a player places a card down, it &quot;battles&quot; its adjacent cards. If the number on the card facing another card is higher, that player captures the adjacent card. (It is important to note that only one card can be captured at once, with adjacent cards checked in counter-clockwise order). The player with the most captured cards at the end wins. Good luck!!! " Height="347" Width="442" TextWrapping="Wrap" FontFamily="Unispace" FontSize="15"/>
                    <Button Content="Continue" Click="Continue_Click" Margin="0,20,0,0" FontFamily="Neo Sans" Background="#FFC8C9FF" Width="113" FontSize="20" Height="32"/>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>

</UserControl>
